pipeline {
    agent any

    stages {
        stage('Git Checkout') {
            steps {
                // Checkout the code from the Git repository
                git 'https://github.com/whoami-anoint/Jenkins_Multibranch.git'
            }
        }
    }
}
